"""Test suite for the mcp_agent.enhancements package.

Covers:
  * Protocol negotiation + compat layer (P1.1)
  * A2A client/server + hybrid gateway (P1.2)
  * Adaptive streaming + multiplexer (P2.1)
  * Connection pool + circuit breaker + quota (P2.2, P4.1)
  * Plugin manager + hot-reload (P3.1)
  * Workflow pattern registry + composer (P3.2)
  * Resilient executor + fallback + state recovery (P4.1)
  * Health monitor + autoscaler (P4.2)
  * Cross-cutting E2E scenario
  * Performance regression benchmark vs baseline
"""
